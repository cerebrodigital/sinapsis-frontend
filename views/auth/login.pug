extends ../layout
block head-scripts
  link(rel='stylesheet', href='/css/login.css')
block bottom-scripts
  script(src="/js/login.js")

block content
  .container
    .omb_login
      h3.omb_authTitle
        | Conectate a la comunidad
      .row.omb_row-sm-offset-3
        .col-xs-12.col-sm-6
          form#conectar.omb_loginForm(action='', autocomplete='off', method='POST')
            .input-group
              span.input-group-addon
                i.fa.fa-user
              input.form-control(type='email', name='email', placeholder='Email / Correo Electrónico')
            span.help-block
            .input-group
              span.input-group-addon
                i.fa.fa-lock
              input.form-control(type='password', name='password', placeholder='Contraseña')
            br
            button.btn.btn-lg.btn-primary.btn-block(type='submit') Conectar
      .row.omb_row-sm-offset-3
        .col-xs-12.col-sm-3
          label.checkbox
            input(type='checkbox', value='remember-me')
            | Recuerdame
        .col-xs-12.col-sm-3
          p.omb_forgotPwd
            a(href='#') ¿Olvidaste tu contraseña?
