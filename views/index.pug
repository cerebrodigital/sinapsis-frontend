extends layout
block page-scripts
  script(src="/javascripts/main.js")

block content
  h1= title
  p Welcome to #{title}

  #app
    |{{ (2 + 51 ) + message + " + html text + " + message + (12 - 5)}}

